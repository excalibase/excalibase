apiVersion: v1
kind: Service
metadata:
  name: provisioning
  namespace: {{ .Values.namespace }}
  labels:
    app: provisioning
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: provisioning
